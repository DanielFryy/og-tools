import React from "react";

import DefensesPage from "./DefensesPage";
import { render } from "setupTests";

describe("DefensesPage", () => {
  it("renders with default props", () => {
    render(<DefensesPage />);
  });
});
