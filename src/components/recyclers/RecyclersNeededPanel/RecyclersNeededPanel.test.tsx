import React from "react";

import RecyclersNeededPanel from "./RecyclersNeededPanel";
import { render } from "setupTests";

describe("RecyclersNeededPanel", () => {
  it("renders with default props", () => {
    render(<RecyclersNeededPanel />);
  });
});
