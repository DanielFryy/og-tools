import React from "react";

import LegendWithoutRange from "./LegendWithoutRange";
import { render } from "setupTests";

describe("LegendWithoutRange", () => {
  it("renders with default props", () => {
    render(<LegendWithoutRange />);
  });
});
