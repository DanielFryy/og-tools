import React from "react";

import InputsSection from "./InputsSection";
import { render } from "setupTests";

describe("InputsSection (planet-size)", () => {
  it("renders with default props", () => {
    render(<InputsSection />);
  });
});
